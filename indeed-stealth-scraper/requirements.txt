# Indeed Stealth Scraper — Python dependencies
# Install with: pip install -r requirements.txt
# Then run: playwright install chromium

# Scraper core
playwright>=1.44.0
httpx>=0.27.0

# Web UI (FastAPI + server)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
aiofiles>=24.1.0

# Export
openpyxl>=3.1.0
reportlab>=4.2.0

# IT classifier — semantic similarity (optional; ~90 MB model download on first use)
sentence-transformers>=3.0.0
